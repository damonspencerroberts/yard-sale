<div class="container">
  <%= render partial: "profile-header" %>
  <div>
    <ul class="list-inline tabs-underlined">
      <li>
        <%= link_to "Profile", user_profile_path, class: "tab-underlined" %>
      </li>
      <li>
        <%= link_to "My bookings", user_profile_bookings_path, class: "tab-underlined active" %>
      </li>
      <li>
        <%= link_to "Listed backyards", user_profile_backyards_path, class: "tab-underlined" %>
      </li>
      <li>
        <%= link_to "Booking Confirmations", user_profile_confirmation_path, class: "tab-underlined" %>
      </li>
      <li>
        <%= link_to "Posted Reviews", user_profile_reviews_path, class: "tab-underlined" %>
      </li>
    </ul>
  </div>
  <div>
    <div class="d-flex justify-content-center align-items-center p-4">
      <h3>Your Bookings</h3>
    </div>
    <div class="my-5">
      <% if @bookings.empty? %>
        <div class="d-flex flex-column justify-content-center align-items-center p-1">
          <div class="py-2">
            <p class="text-danger">
              Currently no bookings! Check out all the backyards below
            </p>
          </div>
          <div>
            <%= link_to "See Backyards", backyards_path, class: "btn btn-success" %>
          </div>
        </div>
      <% else %>
        <% @bookings.each_with_index do |booking, index| %>
          <div class="card-product">
            <%= cl_image_tag @backyards[index].photos.first.key %>
            <div class="card-product-infos">
              <h2><%= @backyards[index].name %></h2>
              <p><%= @backyards[index].description %></p>
              <div class="d-flex flex-column py-2">
                <%= link_to "Edit Booking", edit_backyard_booking_path(@backyards[index], booking) %>
                <%= link_to "Delete Booking", backyard_booking_path(@backyards[index], booking), method: :delete, data: { confirm: 'Are you sure?'} %>
              </div>
            </div>
            <div class="p-4">
              <% if booking.confirmed %>
                <div class = "confirmed">
                  <span>Confirmed</span>
                </div>
              <% elsif booking.confirmed == false %>
                <div class = "declined">
                  <span>Declined</span>
                </div>
              <% else %>
                <div class = "pending">
                  <span>Pending</span>
                </div>
              <% end %>
              <div>
                <div>
                  <p>Date: <%= booking.date %></p>
                </div>
                <div>
                  <p>Guests: <%= booking.number_of_guests %></p>
                </div>
                <div>
                  <p>Cost: £<%= @backyards[index].price %></p>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>